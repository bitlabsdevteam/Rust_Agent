{
  "name": "retry continues after a recoverable observation",
  "user_input": "retry the web lookup",
  "observations": [
    "Recoverable tool failure from `web_search_tool`: timeout"
  ],
  "expected": {
    "action": "retry",
    "failure_class": "recoverable",
    "reason_contains": "recoverable failure"
  }
}
